import { RoundedCornerDirective } from './rounded-corner.directive';

describe('RoundedCornerDirective', () => {
  it('should create an instance', () => {
    const directive = new RoundedCornerDirective();
    expect(directive).toBeTruthy();
  });
});
